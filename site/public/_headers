# hechima-wasm は -pthread ビルド = SharedArrayBuffer 必須。
# サイト全体を cross-origin isolated にする（外部リソースは埋め込まない = 自己完結）。
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
