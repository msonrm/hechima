{
  "name": "hechima-lab-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-versions.mjs && tsc --noEmit && vite build && node scripts/gzip-dict.mjs",
    "check": "node scripts/check-versions.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
